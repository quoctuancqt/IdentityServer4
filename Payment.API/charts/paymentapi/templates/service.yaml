apiVersion: v1
kind: Service
metadata:
  name: {{ template "paymentapi.fullname" . }}
  labels:
    app: {{ template "paymentapi.name" . }}
    chart: {{ template "paymentapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "paymentapi.name" . }}
    release: {{ .Release.Name }}
